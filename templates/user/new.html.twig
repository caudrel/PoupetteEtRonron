{% extends 'base_admin.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    {% if is_granted('ROLE_SUPER_ADMIN') %}

        <div class="pr-admin-header">
            <h1 class="pr-color-orange mt-0 mb-5">Utilisateur</h1>
        </div>

        <div class="pr-box w-50 m-auto p-3 d-flex flex-column align-items-center">
            <h3 class="mt-3 mb-4">Ajouter</h3>
            {{ include('user/_form_new.html.twig') }}
        </div>

<div class="d-flex justify-content-center mt-4">
        <button class="pr-button-ice"><a href="{{ path('app_user_index') }}">Annuler</a></button>
</div>

    {% endif %}
{% endblock %}
