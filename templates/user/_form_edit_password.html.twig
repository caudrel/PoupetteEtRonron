{{ form_start(form) }}


<div class="col">
    <input
            name="{{ field_name(form.password.first) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Mot de passe" required>
    <div class="pr-form-error">
        {{ form_errors(form.password) }}
    </div>
</div>

<div class="col">
    <input
            name="{{ field_name(form.password.second) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Confirmer votre mot de passe" required>
    <div class="pr-form-error">
        {{ form_errors(form.password) }}
    </div>
</div>

<div class="d-flex justify-content-center">
<button class="btn pr-button-slate">{{ button_label|default('Save') }}</button>
</div>

{{ form_end(form) }}
