{% extends 'base_admin.html.twig' %}

{% block title %}Edit Opening Hours{% endblock %}

{% block body %}

    <div class="pr-admin-header">
        <h1 class="pr-color-orange mt-0 mb-5">Horaires d'ouvertures</h1>
    </div>

    <div class="w-50 m-auto p-3 d-flex flex-column align-items-center pr-box">
    <h3 class="mt-3 mb-4">Modifier un horaire</h3>
    {{ include('hours/_form_edit.html.twig', {'button_label': 'Mettre à jour'}) }}

    <button class="btn pr-button-ice"><a href="{{ path('app_hours') }}">Retour à la liste</a></button>

    {{ include('hours/_delete_form.html.twig', {'id': openingHours.id}) }}
    </div>

{% endblock %}