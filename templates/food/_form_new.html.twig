{{ form_start(form) }}

<div class="col">
    <div class="">
        {{ form_widget(form.foodCategory, {'attr': {'class': 'form-select pr-admin-form-input'}, 'placeholder': 'Sélectionnez une categorie'}) }}
    </div>
    {#<input
            name="{{ field_name(form.foodCategory) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Catégorie">#}
    <div class="pr-form-error">
        {{ form_errors(form.foodCategory) }}
    </div>

    {#  {{ form_row(form.foodCategory, {'label_attr' : {'class':'sr-only'}, 'attr': {'class': ''}}) }}
        {{ form_errors(form.foodCategory) }} #}
</div>


<div class="col">
    <input
            name="{{ field_name(form.foodName) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Question">
    <div class="pr-form-error">
        {{ form_errors(form.foodName) }}
    </div>
</div>

<div class="col">
    <input
            name="{{ field_name(form.description) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Réponse">
    <div class="pr-form-error">
        {{ form_errors(form.description) }}
    </div>
</div>

<div class="col">
    <input
            name="{{ field_name(form.price) }}"
            type="number" class="form-control pr-admin-form-input"
            placeholder="Prix en euros (ex: 6.90 )">

    <div class="pr-form-error">
        {{ form_errors(form.price) }}
    </div>
</div>

{#<div class="col">
    <div class="d-flex justify-content-between">
        <div class="">{{ form_label(form.price) }}</div>
        <div class="">{{ form_widget(form.price) }} €</div>

    </div>
</div>#}

<div class="col">
    <div class="d-flex justify-content-between">
        <div class="me-3">{{ form_widget(form.isVegetarian) }}</div>
        <div class="w-100 mb-5">{{ form_label(form.isVegetarian) }}</div>
    </div>
</div>

<div class="col">
    <div class="d-flex justify-content-between">
        <div class="me-3">{{ form_widget(form.isActiv) }}</div>
        <div class="w-100 mb-5">{{ form_label(form.isActiv) }}</div>
    </div>
</div>


<div class="d-flex justify-content-center">
    <button class="btn pr-button-slate">{{ button_label|default('Valider') }}</button>
</div>

{{ form_end(form) }}
