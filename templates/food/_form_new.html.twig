{{ form_start(form) }}

<div class="col">
    <div class="">
        {{ form_widget(form.foodCategory, {'attr': {'class': 'form-select pr-admin-form-input'}, 'placeholder': 'Sélectionnez une categorie'}) }}
    </div>
    <div class="pr-form-error">
        {{ form_errors(form.foodCategory) }}
    </div>

</div>


<div class="col">
    <input
            name="{{ field_name(form.foodName) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Nom du plat ou du menu">
    <div class="pr-form-error">
        {{ form_errors(form.foodName) }}
    </div>
</div>

<div class="col">
    <input
            name="{{ field_name(form.description) }}"
            type="text" class="form-control pr-admin-form-input"
            placeholder="Description">
    <div class="pr-form-error">
        {{ form_errors(form.description) }}
    </div>
</div>

<div class="col">
    {{ form_widget(form.price, {'attr': {'class': 'form-control pr-admin-form-input', 'placeholder': 'Prix en euros (ex: 6.90 )'}}) }}
    <div class="pr-form-error">
        {{ form_errors(form.price) }}
    </div>
</div>

<div class="col">
    <div class="d-flex justify-content-between">
        <div class="me-3">{{ form_widget(form.isVegetarian) }}</div>
        <div class="w-100 mb-1">{{ form_label(form.isVegetarian) }}</div>
    </div>
</div>

<div class="col">
    <div class="d-flex justify-content-between">
        <div class="me-3">{{ form_widget(form.isActiv) }}</div>
        <div class="w-100 mb-3">{{ form_label(form.isActiv) }}</div>
    </div>
</div>


<div class="d-flex justify-content-center">
    <button class="btn pr-button-slate">{{ button_label|default('Valider') }}</button>
</div>

{{ form_end(form) }}
