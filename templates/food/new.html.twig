{% extends 'base_admin.html.twig' %}

{% block title %}Ajouter un plat ou un menu{% endblock %}

{% block body %}

    <div class="pr-admin-header">
        <h1 class="pr-color-orange mt-0 mb-5">Plats et Menus</h1>
    </div>

    <div class="pr-box w-50 m-auto p-3 d-flex flex-column align-items-center">
        <h3 class="mt-3 mb-4">Ajouter</h3>
        {{ include('food/_form_new.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <div class="d-flex justify-content-center mt-3">
        <a href="{{ path('app_food') }}"><button class="btn pr-button-ice">Annuler</button></a>
    </div>

{% endblock %}