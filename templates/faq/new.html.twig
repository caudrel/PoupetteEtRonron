{% extends 'base_admin.html.twig' %}

{% block title %}FAQ{% endblock %}

{% block body %}

    <div class="pr-admin-header">
        <h1 class="pr-color-orange mt-0 mb-5">FAQ</h1>
    </div>

    <div class="pr-box w-50 m-auto p-3 d-flex flex-column align-items-center">
    <h3 class="mt-3 mb-4">Ajouter</h3>
    {{ include('faq/_form_new.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <div class="d-flex justify-content-center mt-3">
        <a href="{{ path('app_faq') }}"><button class="btn pr-button-ice">Annuler</button></a>
    </div>

{% endblock %}