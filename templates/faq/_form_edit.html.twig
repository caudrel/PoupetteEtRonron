
{{ form_start(form) }}


<div class="col">
    <label class="">Question</label>
    <input
            name="{{ field_name(form.question) }}"
            type="text" class="form-control pr-admin-form-input"
            value="{{ faq.question }}"
            required>
    <div class="pr-form-error">
        {{ form_errors(form.question) }}
    </div>
</div>

<div class="col">
    <label class="">Réponse</label>
    <input
            name="{{ field_name(form.answer) }}"
            type="text" class="form-control pr-admin-form-input"
            value="{{ faq.answer }}"
            required>
    <div class="pr-form-error">
        {{ form_errors(form.answer) }}
    </div>
</div>

<h5 class="mt-5 mb-3">Cette question doit-elle s'afficher en ce moment ?</h5>
<div class="d-flex justify-content-between">
    <div class="me-3">{{ form_widget(form.isActiv) }}</div>
    <div class="w-100 mb-5">{{ form_label(form.isActiv) }}</div>
</div>


<div class="d-flex justify-content-center">
<button class="btn pr-button-slate">{{ button_label|default('Valider') }}</button>
</div>

{{ form_end(form) }}
