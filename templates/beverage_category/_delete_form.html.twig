<form method="post" action="{{ path('app_beverage_category_delete', {'id': beverageCategory.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette Catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beverageCategory.id) }}">
    <button class="btn pr-button-orange">Supprimer</button>
</form>