
{{ form_start(form) }}


<div class="col">
    <label class="">Catégorie de boissons</label>
    <input
            name="{{ field_name(form.beverageCategoryName) }}"
            type="text" class="form-control pr-admin-form-input"
            value="{{ beverageCategory.beverageCategoryName }}" required>
    <div class="pr-form-error">
        {{ form_errors(form.beverageCategoryName) }}
    </div>
</div>

<h5 class="mt-5 mb-3">Cette catégorie de boisson doit-elle s'afficher en ce moment ?</h5>
<div class="d-flex justify-content-between">
    <div class="me-3">{{ form_widget(form.isActiv) }}</div>
    <div class="w-100 mb-5">{{ form_label(form.isActiv) }}</div>
</div>


<div class="d-flex justify-content-center">
    <button class="btn pr-button-slate">{{ button_label|default('Valider') }}</button>
</div>

{{ form_end(form) }}
