<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Reset Password Request{% endblock %}</title>
    <link rel="icon" type="image/png" href="{{ asset('favicon.ico') }}"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400&family=Palanquin&family=Permanent+Marker&display=swap"
          rel="stylesheet">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>

<body>

<div class="pr-container-admin-all">
    <div class="pr-container-admin-nav">
        {% include "includes/_navbar_admin.html.twig" %}
    </div>

    <div class="pr-container-admin-body">

        {% for category, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ category }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}



        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}

        <div class="d-flex my-auto h-75">
        <div class="pr-box w-75 m-auto p-3 d-flex flex-column align-items-center">
            <h3 class="mt-3 mb-4">Réinitialiser votre mot de passe</h3>

            {{ form_start(requestForm) }}

            <input
                    name="{{ field_name(requestForm.email) }}"
                    type="text" class="form-control pr-admin-form-input"
                    placeholder="Email" required>
            <div class="pr-form-error">
                {{ form_errors(requestForm.email) }}
            </div>

            <div>
                <small>
                    Entrez votre adresse email, et nous vous enverrons un lien pour réinitialiser votre mot de passe.
                </small>
            </div>

            <div class="d-flex justify-content-center">
            <button class="btn pr-button-slate w-25 mt-5">Envoyer la demande</button>
            {{ form_end(requestForm) }}
            </div>
        </div>
        </div>

    </div>
</div>
</body>

</html>