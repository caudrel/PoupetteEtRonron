{% extends 'base_admin.html.twig' %}

{% block title %}Food Categories{% endblock %}

{% block body %}

    <div class="pr-admin-header">
        <h1 class="pr-color-orange mt-0 mb-5">Catégories de plats</h1>
    </div>

    <div class="w-50 m-auto p-3 d-flex flex-column align-items-center pr-box">
        <h3 class="mt-3 mb-4">Modifier</h3>
        {{ include('food_category/_form_edit.html.twig', {'button_label': 'Mettre à jour'}) }}
        <button class="btn pr-button-ice"><a href="{{ path('app_food_category') }}">Retour à la liste</a></button>

        {{ include('food_category/_delete_form.html.twig', {'id': foodCategory.id}) }}
    </div>



{% endblock %}