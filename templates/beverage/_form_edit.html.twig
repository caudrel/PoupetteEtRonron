{{ form_start(form) }}

<div class="col">
    <div class="">
        {{ form_widget(form.beverageCategory, {'attr': {'class': 'form-select pr-admin-form-input'}}) }}
    </div>
    <div class="pr-form-error">
        {{ form_errors(form.beverageCategory) }}
    </div>
</div>

<div class="col">
    <div class="">
        {{ form_widget(form.beverageName, {'attr': {'class': 'form-control pr-admin-form-input'}}) }}
    </div>
    <div class="pr-form-error">
        {{ form_errors(form.beverageName) }}
    </div>
</div>

<div class="col">
    <div class="">
        {{ form_widget(form.description, {'attr': {'class': 'form-control pr-admin-form-input'}}) }}
    </div>
    <div class="pr-form-error">
        {{ form_errors(form.description) }}
    </div>
</div>


<div class="col">
    {{ form_widget(form.price, {'attr': {'class': 'form-control pr-admin-form-input'}}) }}
    <div class="pr-form-error">
        {{ form_errors(form.price) }}
    </div>
</div>

<div class="col">
    <div class="d-flex justify-content-between">
        <div class="me-3">{{ form_widget(form.isActiv) }}</div>
        <div class="w-100 mb-3">{{ form_label(form.isActiv) }}</div>
    </div>
</div>

<div class="d-flex justify-content-center">
    <button class="btn pr-button-slate">{{ button_label|default('Valider') }}</button>
</div>

{{ form_end(form) }}
